# 工作包 000：管線煙霧測試

## 目標

驗證派工管線可用。這是一次性測試，產出隨後會被刪除。

## 要做的事

在專案根目錄建立檔案 `SMOKE.md`，內容**只有**這一行：

```
pipeline-ok
```

## 驗收條件

- `SMOKE.md` 存在且內容正好是 `pipeline-ok`
- 你必須實際 `cat` 它一次，把真實輸出貼出來
- 不要建立、修改任何其他檔案
