# 工作包 037a-2 — 差集比對（**續 037a 的同一個 session，仍然唯讀**）

## 這一包在做什麼

037a 你已經交付了報告，其中 §1 是**你自己列的調查面向**。

現在給你主對話那份**刻意不給你**的清單。這一包只做一件事：
**兩份清單做差集，然後補上「我有、你沒有」的那幾項。**

🔴 **仍然唯讀，仍然一個字都不准改 repo 裡的程式碼。**
🔴 **不准回頭修改 REPORT.md 的 §1～§4。** §1 是「你在看清單之前的判斷」，
改了它這個機制就毀了——它的價值正是「當時漏了什麼」。

---

## 1. 主對話當初列的清單（完整，共 8 項）

這是主對話在寫 037a 工作包時心裡的那份，**逐字如下，沒有事後增刪**：

1. **兩支 shell 腳本裡的指令與參數在 BSD 版是否等價**（`find`／`date`／`grep`／`stat` 等）
2. **Python 的平台相依 API**（`os.path`、`shutil.which`、`subprocess`、`signal`、`socket`）
3. 🔴 **`start.sh` 偵測「埠已經有人在聽」的機制在 macOS 是否等價**
   （035 那包做的事；用什麼方式偵測？該方式在 macOS 行為相同嗎？）
4. **`webbrowser` 開瀏覽器的行為**
5. **路徑假設**（硬編碼的絕對路徑、家目錄形狀、`/usr/bin` vs `/opt/homebrew/bin`）
6. **bash 版本相依語法**（macOS 內建 3.2）
7. **檔案編碼與 locale**
8. 🔴 **桌面啟動路徑**：Linux 用 `.desktop` 檔，**macOS 沒有這個東西**
   ⇒ 從 Dock／Finder 啟動的等價方案是什麼？以及**它不讀 `.zshrc`** 造成的 `PATH` 問題

---

## 2. 你要做的事

### 2.1 先做差集，明確分成三類

在 `REPORT.md` **末尾追加 `## §5 差集比對`**，分三個小節逐項列出：

| 小節 | 內容 |
|---|---|
| **§5.1 兩邊都有** | 你的 §1 與上面清單重疊的項目（對照編號，一句話帶過即可） |
| **§5.2 你有、主對話沒有** | 🔴 **這是你的產出價值，逐項寫清楚**——主對話漏了什麼 |
| **§5.3 主對話有、你沒有** | 你當初漏掉的，逐項列出**並在 §6 補查** |

⚠️ **§5.2 不要客氣也不要灌水。** 你在 037a 找到的東西如果不在主對話清單裡，
就直說「這一項你沒列到」。**這個機制存在的理由就是抓雙向的盲點。**

### 2.2 補查 §5.3 的每一項

在 `REPORT.md` 追加 `## §6 補查（原本漏掉的面向）`，
**每一項用 037a §2 的同一個格式**（位置／證據等級／現況／疑慮／依據／建議修法／嚴重度）。

🔴 **證據等級的紀律完全不變**：
`[Linux實測]`（附輸出）／`[文件推論]`（附依據）／`[需Mac實測]`（附指令與判定標準）。
**你沒有 Mac，任何 macOS 行為都不准標成 `[Linux實測]`。**

### 2.3 特別提醒兩項

- **清單第 3 項（埠偵測）**：先去 `start.sh` 讀出它到底用什麼方式偵測，
  再判斷那個方式在 macOS 上是否等價。**不要憑印象猜它用什麼**。
- **清單第 8 項（桌面啟動）**：Linux 那個 `.desktop` 檔**刻意不在 repo 裡**
  （它必須寫絕對路徑與使用者名稱，這是 public repo 禁止的內容）。
  ⇒ 你**看不到那個檔**，這是正常的。這一項要回答的是
  **「macOS 上的等價機制是什麼、有什麼已知的 `PATH` 陷阱」**，屬 `[文件推論]`。
  **不要為了這一項去建立任何檔案。**

---

## 3. 🔴 不准做的事（與 037a 相同，一條都沒放寬）

- **不准修改 repo 裡的任何檔案。** 唯一准許寫入的路徑仍是 `dispatch/tmp/037a/`。
- **不准修改 REPORT.md 的 §1～§4**，只准在末尾**追加** §5 與 §6。
- **不准執行任何版控寫入指令**（`git add`／`commit`／`checkout`／`stash`／`restore`）。
  唯讀查詢（`git status`／`log`／`diff`／`grep`／`ls-files`）可以用。
- **不准呼叫任何 AI CLI**（`claude`／`codex`／`gemini`／`opencode`）。
- **不准跑 `--live`**。
- **不准碰 8765 埠**（Frank 可能有伺服器在上面跑）。需要起伺服器就用 **8791**。
- 臨時腳本一律放 `dispatch/tmp/037a/`。

---

## 4. 驗證（強制，逐項附實際輸出）

```bash
git status --short
```
🔴 **必須是完全空的**（`dispatch/tmp/` 被 gitignore，報告不會出現在這裡，這是預期的）。

```bash
python3 -m unittest discover tests
```
**應為 `Ran 339 tests` ＋ `OK`。**

```bash
ps -eo comm,args | awk '$1=="python3" && /serve\.py/'
```
**應為空**（若你為了驗證起過伺服器，收工前必須關掉）。

---

## 5. 交付前自己確認

- [ ] `REPORT.md` 的 §1～§4 **一個字都沒改**
- [ ] 追加了 §5（三個小節都有內容）與 §6
- [ ] §5.2「你有、主對話沒有」有據實填寫，沒有為了客氣而省略
- [ ] §6 每一項都有證據等級，且沒有任何 macOS 行為被標成 `[Linux實測]`
- [ ] 第 3 項（埠偵測）是**讀了 `start.sh` 之後**回答的，不是憑印象
- [ ] 第 8 項（桌面啟動）**沒有建立任何檔案**
- [ ] `git status --short` → 空白；測試 → 339 OK；`ps` → 空。三項都附實際輸出
- [ ] 全程沒碰 8765、沒 `--live`、沒呼叫 AI CLI、沒執行版控寫入指令

---

## 6. 卡住怎麼辦

契約有矛盾就寫 `dispatch/BLOCKED.md`，不要自己選一個讀法硬做。
