# BLOCKED · 工作包 030（2026-08-06）

狀態：**停止實作，等待裁示**。三檔改動已完成、備份在 `dispatch/tmp/030-backup/`，
新增的五條測試全過；**但有一條既有測試因 `AGENT_DEF` 改動翻紅**，依工作包
「既有測試一條都不得修改或刪除」的紅線，停手回報。

## ① 卡在哪一句需求

工作包 030 §B 同時要求：

- 🔴 既有測試**一條都不得修改或刪除**。
- 🔴 若有既有測試因為 `AGENT_DEF` 改動而翻紅 ⇒ **停手、寫 `dispatch/BLOCKED.md` 回報**。
- 驗收條件 1：既有 314 個測試一個都不得變紅，新增五條 ⇒ 總數 319、全過。

實測（`python3 -m unittest discover tests`）：**319 條中 1 條翻紅**：

```
FAIL: test_agent_file_created_with_deny (test_adapters_ask.OpenCodeAskTest.test_agent_file_created_with_deny)
AssertionError: 'bash: deny' not found in '---\ndescription: Read-only council advisor. Web search allowed, nothing else.\nmode: primary\npermission:\n  "*": deny\n  websearch: allow\n---\n\nYou are a council advisor. Answer the question directly.\n'
```

翻紅測試在 `tests/test_adapters_ask.py:245-266`：它模擬 `ask()` 把暫存目錄裡寫出的
agent 檔複製出來，斷言內容含 `"bash: deny"`（265 行）與 `"edit: deny"`（266 行）。
新 `AGENT_DEF` 把逐項 deny 換成 `"*": deny` 後單開 `websearch: allow`（SPEC §4.2 明訂
的萬用字元寫法），這兩行**必然消失**——這正是本包要的結果，但這條既有測試的文字斷言
沒有跟上。**改壞的是測試與新規格脫節，不是 `AGENT_DEF` 本身有誤。**

我新增的五條 `OpenCodeAgentPermissionTest` 全部通過，沒有一條碰過既有測試。

## ② 我想到的所有解讀

1. **更新 `test_agent_file_created_with_deny` 的斷言內容**（准許動這唯一一條既有測試）：
   把 `assertIn("bash: deny", content)`／`assertIn("edit: deny", content)` 改成斷言新方案
   ——例如 `assertIn('"*": deny', content)`、`assertIn("websearch: allow", content)`，
   並把方法名從 `..._with_deny` 改為反映「全關再單開」的名字。這條測試的精神是
   「驗證 `ask()` 實際寫進暫存目錄的那份 agent 檔內容正確」，精神不變，只是期望值
   跟著規格走。與我新增的 `OpenCodeAgentPermissionTest`（直接斷言 `AGENT_DEF`）互補。
2. **保留既有斷言、把逐項 deny 加回去**：讓 `AGENT_DEF` 同時含 `"*": deny` 與全部逐項
   deny。這與 SPEC §4.2 及工作包「不要加回任何逐項 deny」直接衝突，且逐項清單遲早
   漏掉 opencode 新增的工具，等於否定本包的意義。**不可行**。
3. **刪掉這條既有測試**：工作包明令禁止，不可行。

## ③ 我需要的那個決定

請裁示解讀 1 是否成立：**是否准許把 `test_agent_file_created_with_deny` 的內容斷言
更新為新方案的期望值**（方法名同步更名，測試仍維持「驗證 `ask()` 實際寫出的檔案」的
原意）。若准，收尾計畫：改該條測試 → 重跑全測（應 319 全過）→ 完成剩餘驗收產物
（git diff、突變驗證、掃描）。

## ④ 若硬要往下做、我會選哪個解讀

選解讀 1。理由：

- 這是唯一能讓「驗證寫入檔案的既有測試」與 SPEC §4.2 新契約同時成立的選項；
- 翻紅的根因是「舊測試斷言舊格式」，不是新方案有缺陷——SPEC 已明訂萬用字元寫法，
  且工作包自己承認「改壞了不會有人叫」是因為過去零覆蓋，新增的
  `OpenCodeAgentPermissionTest` 就是來守這塊的；
- 選 2 直接違反 SPEC 與工作包文字，選 3 違反「一條都不得刪」。

（以上為 fallback 方案，**未執行**。既有測試一字未動，其他兩檔改動已在工作區待審。）
