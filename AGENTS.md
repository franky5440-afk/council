# council 專案規則

本檔是 council 專案的**唯一**規則檔，builder 與 reviewer 共用同一份標準。
全域 `~/.config/opencode/AGENTS.md` 仍然生效；**本檔與全域衝突時，以本檔為準**。

⚠️ 根目錄另有一個 `CLAUDE.md`，那是 Claude Code 在專案內開 session 時的**自動載入器**，
**刻意不含任何規則**（只有讀取順序、角色、常用指令）。**它不是第二份規則檔**——
規則一律以本檔為準，實作契約一律以 `SPEC.md` 為準。若哪天發現 `CLAUDE.md` 裡長出了
規則，那是錯誤，照 BLOCKED 流程回報。

---

## 角色分工

| 角色 | 是誰 | 負責 |
|---|---|---|
| 需求方 | Frank | 決定方向、驗收、授權 push |
| 派工／審查 | Claude Code 主對話 | 寫工作包、審 diff、縫合、版控 |
| builder | opencode + deepseek | 依工作包實作與自我驗證 |

Frank 不轉傳工作包，主對話直接以 `opencode run` 派工給你。

---

## ⚠️ 你是被非互動呼叫的（最重要的一節）

你在本專案是透過 `opencode run --dir` 以 **headless 模式**執行，因此有三件事你必須知道（2026-08-03 實測）：

1. **你無法反問。** `question` 權限為 `deny`，你提出的問題不會有人回答。
2. **⚠️ 你並沒有被關在 council 目錄內。** 需要批准的操作會出現 `external_directory`
   提示並被自動拒絕，但那道閘門**對 bash 寫入無效**——實測寫檔到專案外會成功
   （證據見 `SPEC.md` §4.2）。因此「不碰 council 以外的檔案」對你是**紀律，不是護欄**，
   沒有任何機制會攔你。這條先前被寫成「會被自動拒絕」，那是錯的。
3. **沒有人在旁邊按同意。** 需要確認的操作會被自動拒絕，不是等待。

### 因此：遇到歧義不要猜，改用 BLOCKED 回報

全域規則寫「需求有多種解讀 → 全部列出來問我」。在本專案**那個動作做不到**，改成這樣：

> 停止實作，建立或覆寫 `dispatch/BLOCKED.md`，寫明：
> ① 卡在哪一句需求 ② 你想到的所有解讀 ③ 你需要的那個決定是什麼
> ④ 若硬要往下做、你會選哪個解讀（讓主對話能快速裁示）
> 然後結束該輪，不要動其他檔案。

主對話每輪都會檢查這個檔案。**猜錯方向做完一整包，比停下來貴得多。**

---

## 版控：你不要碰

**不要執行 `git add`／`git commit`／`git push`／建立分支／切換分支。**

這一條**覆寫全域規則**「Git 例行操作可自行執行」。原因：主對話的審查方式是比對「派工前 HEAD」與「你改完之後的工作區」，你自行 commit 會把這條基準線打亂，審查就失去依據。

把改動留在工作區即可，版控由主對話負責。

---

## 交付標準

- **先動手再解釋**：直接改檔案，不要把程式碼貼出來讓人自己複製。
- **自我驗證是交付的一部分**：實際執行過再回報，並貼出真實輸出。
- **沒驗證就標「未驗證」**，不要用有把握的語氣描述你沒跑過的東西。這是本專案對你最看重的一點。
- 臨時腳本放在專案內明確位置（例如 `dispatch/tmp/`），不要散落到 `/tmp`。（⚠️ 你**寫得進** `/tmp`，見上面第 2 點——這條靠你遵守。）

## 不要做的事

- 不要修改 `AGENTS.md`（本檔）或 `dispatch.sh`。需要調整規則或派工機制，寫進 `dispatch/BLOCKED.md` 提出。
- 不要碰 council 以外的檔案。
- 不要新增沒被要求的功能、抽象層、設定項。

## 程式碼準則

沿用全域 `AGENTS.md` 的最小化（KISS）、外科手術式精準、目標導向三節，此處不重複。

---

## 專案方向

council 是一個本機 orchestrator：讓使用者在同一個對話框裡徵詢 3～4 個 AI，
彼此看得見、輪流發言，最後由指定的仲裁者整合結論。
**它驅動使用者自己安裝並登入的官方 CLI 子行程，不串接任何模型 API。**

**規格見 `SPEC.md`，那是實作的唯一契約來源。動手前先讀它。**

工作包只會引用 `SPEC.md` 的章節，不重述內容。若工作包與 `SPEC.md` 衝突，
**不要自行選一邊**——那是規格有問題，照上面的 BLOCKED 流程回報。

### 對本專案特別要求

- **停止邊界（`SPEC.md` §5）是本專案最重要的部分。** 它保護的是使用者的訂閱額度。
  凡是會發出 CLI 呼叫的程式碼，機制層的邊界（逾時、上限、不自動續輪）不得省略，
  也不得改用「在 prompt 裡請模型自我克制」代替。
- **不要在測試中真的呼叫各家 CLI。** 那會消耗真實額度。adapter 測試一律用假的
  子行程（例如指向一個回傳固定輸出的腳本），真實呼叫由主對話人工驗證。
- 專案將公開發布 ⇒ 不得寫入任何本機絕對路徑、使用者名稱、或個人資訊。
